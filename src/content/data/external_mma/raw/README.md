# Raw external snapshots

Raw provider exports are immutable and content-addressed when collection runs
with `--store-raw`. They are intentionally ignored by Git because a provider
license may permit analysis without redistribution. Keep this directory in
durable private storage if external data becomes part of the production model.
